#include<bits/stdc++.h>

int main() {
        int n,d,num;    
        cin>>n>>d;

        vector<int> a(n) ;
        vector<int> a2 ;

        for(auto i=0;i<n;i++) {
                cin>>a[i];
        }

        

return 0;
}